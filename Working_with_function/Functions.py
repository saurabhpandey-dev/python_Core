##Functions are reusable pieces of code that take inputs (arguments) and
##returns an output. To call a function, you need to reference the function
##name followed by a set of parenthesis:
# Defining a function


def get_sum(num_1, num_2):
    return num_1 + num_2

result = get_sum(3, 4) # function call
print(result) # 7
