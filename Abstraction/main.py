import frist as f 
import second as s

print(f.add(10,5))  #15
print(s.sub(10,5))  #5


# an abstract class is a class that cannot be instantiated on its own and is 
# designed to be a blueprint for other classes. Abstract classes allow us to 
# define methods that must be implemented by subclasses, ensuring a consistent 
# interface while still allowing the subclasses to provide specific implementations.