#remove(): Used to remove an item from the list. The remove() method will
#only remove the first occurrence of an item in the list.

numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers) 
