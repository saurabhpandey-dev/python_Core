#pop(): Used to remove a specific item from the list and return it.

numbers = [1, 2, 3, 4, 5]
print(numbers.pop(1)) # The number 2 is returned

#If you don't specify an element for the pop method, then the last element is
#removed.

numbers = [1, 2, 3, 4, 5]
print(numbers.pop())
