#sorted(): A built-in function that returns a new sorted list instead of
#modifying the original list.

numbers = [19, 2, 35, 1, 67, 41]
sorted_numbers = sorted(numbers)

print(sorted_numbers) 
print(numbers) 
