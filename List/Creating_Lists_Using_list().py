#Creating Lists Using the list() constructor: Lists can also be created
#using the list() constructor.
#The list() constructor is used to convert an iterable into a list.

name = 'saurabh'
lst = list(name)

print(name)
print(lst)
